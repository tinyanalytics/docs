---
title: "Identify Users Across Sessions and Devices"
sidebarTitle: "Identify users"
description: "Attach your own stable user ID to visits so TinyAnalytics can follow a person across devices and sessions — cookielessly and exactly."
icon: "user"
---

Call `identify()` with your own user ID to tie a visitor's activity to a known person. This makes people-level reports exact across devices and sessions. A signed-in user on a phone and a laptop counts as **one** user, instead of the two you'd get from cookieless estimation. You choose the ID, and TinyAnalytics keys every people report on it.

```js
window.tinyanalytics.identify('user_12345')
```

## Why identify users?

Without identification, TinyAnalytics counts visitors with a cookieless hash, which is a close estimate that can split one person across devices or merge several people on one network (see [how TinyAnalytics works](/how-tinyanalytics-works#its-an-estimate-and-tinyanalytics-says-so)). When you know who someone is — because they signed in — `identify()` replaces that estimate with your exact ID, so **Users**, **Retention**, and **Cohorts** all agree and follow the real person.

## Identify a signed-in user

Call `identify()` once you know who the visitor is, typically right after they log in:

```js
// ID only
window.tinyanalytics.identify('user_12345')

// An ID plus traits describing the person
window.tinyanalytics.identify('user_12345', {
  plan: 'pro',
  signupDate: '2026-01-15'
})
```

When you call `identify()`, TinyAnalytics:

- Attaches your ID to the visitor's **recent activity** — it backfills the last 30 days of that visitor's events, so their earlier anonymous visits are credited to the person.
- Remembers the ID in the browser (`localStorage`), so the visitor stays identified across page loads and future sessions on that device.
- Merges any `traits` you pass onto the person's profile.

## Which identity methods are available?

TinyAnalytics exposes four identity methods on `window.tinyanalytics`:

| Method | What it does |
| --- | --- |
| `identify(userId, traits?)` | Tie the visitor to your `userId`, backfill their recent events, and save the ID on the device. |
| `setTraits(traits)` | Update the identified person's traits without re-identifying. Pass `null` for a key to delete it. Does nothing if no one is identified yet. |
| `clearUserId()` | Forget the identified user on this device — call it when a user logs out. |
| `getUserId()` | Return the current user ID, or `null`. Survives page reloads. |

```js
// Update traits later
window.tinyanalytics.setTraits({ plan: 'enterprise' })

// On logout
window.tinyanalytics.clearUserId()
```

## What does TinyAnalytics store when you identify a user?

The cookieless hash TinyAnalytics uses for anonymous visitors is never stored on the device and contains no personal data. **Identification is different: the user ID and traits you pass are stored** so they can describe a person over time. Choose what you send accordingly:

- Use a **stable, internal ID** — your database user ID, for example — not something that changes.
- Avoid putting personal data you don't want to keep into the ID or traits. If you don't want to store email addresses, don't pass them.

<Note>
  An `identify()` call fired in the same instant as a pageview may not tag that exact pageview — the backfill applies to events that are already saved. In practice this is a single event at the very start of a visit; all subsequent activity is attributed correctly.
</Note>

## Identify on login, clear on logout

Tie the identity lifecycle to your own session lifecycle:

- **On login**, call `identify(userId, traits?)` as soon as you know who the visitor is.
- **On logout**, call `clearUserId()` so the next person on that device starts anonymous. Skipping this is how two people sharing one browser end up merged under the first person's ID.

## Anonymous and identified modes

TinyAnalytics has two modes. **Anonymous measurement** is the default: cookieless, no analytics ID stored in the browser, and no cookie-consent banner required in most jurisdictions. **Identified mode** starts when you call `identify()`: the user ID and traits you send are personal data, stored so they can describe a person over time.

When you identify users, you are the data controller for that data and owe your users a lawful basis — typically consent. The consent-exempt audience-measurement carve-out that covers anonymous measurement in some jurisdictions does **not** cover cross-referencing identified customer data. See [Privacy & GDPR](/resources/privacy-friendly-analytics-gdpr).

## Choose a write surface

Four server-side writes can change a person's traits. They differ in who may call them and how they treat existing keys:

| Write surface | Auth | Semantics | Use it for |
| --- | --- | --- | --- |
| `POST /api/identify` | None (public tracker ingest) | Merge traits; backfills recent events onto the user ID | The browser `identify()`/`setTraits()` flow |
| `POST /api/sites/{id}/users/identify` | Member session or `users:write` key | Merge traits and backfill a known device's events | Linking a device to a user after the fact |
| `PUT /api/sites/{id}/users/{userId}/traits` | Member session or `users:write` key | **Replace** the whole trait set — omitted keys are deleted | Rebuilding a profile from scratch |
| `PATCH /api/sites/{id}/users/{userId}/traits` | Member session or `users:write` key | **Merge** — keys you send are set, `null` deletes a key, omitted keys are untouched | Connectors and incremental updates |

Connectors and scripts belong on **PATCH**: it is authenticated, attributable to an API key, and never throttled as untrusted ingest. See [Identify API](/api-reference/identify-users-api) and the generated endpoint reference under **Users**.

## Connect a platform (Shopify, CRM, billing)

A platform connector is any process that maps a platform identity to your site's `identify()` user ID and calls the merge write. Follow this contract:

1. **Address users by your own user ID** — the one your site passes to `identify()`. When the platform only knows an email, resolve it with `GET /api/sites/{id}/user-traits/users?key=email&value=…`. Zero or more-than-one matches mean **skip and count** — trait values are not unique, so never guess.
2. **Write with PATCH**, one call per user: `PATCH /api/sites/{id}/users/{userId}/traits` with a `users:write` [API key](/api-reference/api-authentication). Keep the `userId` percent-encoded in the path (IDs like `auth0|abc` round-trip intact). Pace at **10 requests per second or less**, and retry `429` and `5xx` responses with backoff.
3. **Namespace your keys** — prefix connector traits (`shopify_*`, `stripe_*`) so your writes and the app's writes can co-own a profile without stomping each other. The reserved display keys (`username`, `name`, `email`) stay unprefixed. Only scalar values (string, number, boolean) are filterable; arrays and objects are display-only.
4. **Skip unchanged writes.** Compare against your own cursor or read back with `GET /api/sites/{id}/users/{userId}` (the response carries the current `traits`). The store is last-write-wins per key.
5. **Propagate deletions yourself.** Pass `?create=false` so a stale platform ID returns `404` instead of recreating a profile — a default PATCH would silently resurrect a [GDPR-erased](/user-analytics#how-do-i-permanently-delete-a-users-data-gdpr-erasure) user. Treat that `404` as "skip and count."

Errors arrive as [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) problem+json with a machine-readable `code`:

| Status | `code` | Cause and fix |
| --- | --- | --- |
| `400` | `INVALID_USER_ID` | The `userId` path segment is empty or over 255 characters. |
| `400` | `INVALID_TRAITS` | The traits object is malformed or over 2,048 bytes serialized (`cap` names the limit). |
| `403` | — | The key lacks `users:write` — the error names the missing scope. |
| `404` | `USER_NOT_FOUND` | No profile exists and you passed `create=false`. Skip and count, or omit `create=false` to create the profile. |

## Verify it's working

<Steps>
  <Step title="Sign in and identify">
    Trigger your `identify()` call in the browser — usually by logging into your app.
  </Step>
  <Step title="Find the person in Users">
    Open the **Users** report in TinyAnalytics. The visitor now appears under your ID, with their traits and their earlier activity attributed to them.
  </Step>
  <Step title="Confirm a trait key arrived">
    `identify()` swallows network errors by design, so don't treat a quiet console as proof. The honest check is the trait key appearing in `GET /api/sites/{id}/user-traits/keys`, usually within a minute of an identified visit. An agent can run the same probe for you — the [MCP](/api-reference/mcp-server) `check_tracking` tool takes a `trait_key` argument. If the key is missing, check a *later* visit: a pageview fired in the same tick as `identify()` stays anonymous.
  </Step>
</Steps>

<Check>
  Identification is working when the visitor appears in the **Users** report under your own ID, with their traits and their earlier activity attributed to them.
</Check>

## Frequently asked questions

<AccordionGroup>
  <Accordion title="Does identify() apply to activity from before the call?">
    Yes. When you call `identify()`, TinyAnalytics backfills the last **30 days** of that visitor's events, so their earlier anonymous visits are credited to the person. One exception: an `identify()` call fired in the same instant as a pageview may not tag that exact pageview, because the backfill applies to events that are already saved.
  </Accordion>
  <Accordion title="How do I stop identifying a user when they log out?">
    Call `window.tinyanalytics.clearUserId()`. It forgets the identified user on that device. TinyAnalytics otherwise remembers the ID in the browser's `localStorage`, so the visitor stays identified across page loads and future sessions.
  </Accordion>
  <Accordion title="Does TinyAnalytics store the user ID on the visitor's device?">
    Yes. The user ID and any traits you pass to `identify()` are stored so they can describe a person over time, and the ID is kept in the browser's `localStorage`. This differs from the cookieless hash used for anonymous visitors, which is never stored on the device and contains no personal data.
  </Accordion>
  <Accordion title="Do I need identify() to count visitors in TinyAnalytics?">
    No. TinyAnalytics counts visitors with a cookieless hash without any code from you. That count is a close estimate that can split one person across devices or merge several people on one network. Call `identify()` when you know who someone is to replace the estimate with your exact ID.
  </Accordion>
</AccordionGroup>

## Related

<Columns cols={2}>
  <Card title="Track custom events" icon="hand-pointer" href="/custom-event-tracking">
    Send named events that tie to the identified person.
  </Card>
  <Card title="How TinyAnalytics works" icon="diagram-project" href="/how-tinyanalytics-works">
    Cookieless identity and how identify makes counts exact.
  </Card>
  <Card title="Configure the script" icon="sliders" href="/configure-tinyanalytics-tracking-script">
    Control what the script tracks.
  </Card>
  <Card title="Verify your setup" icon="circle-check" href="/verify-tinyanalytics-installation">
    Confirm identification is arriving.
  </Card>
  <Card title="Analytics filters" icon="filter" href="/analytics-filters">
    Filter reports by the traits you attach.
  </Card>
</Columns>
